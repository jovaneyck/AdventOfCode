module App

open System
open InfiniteElves

[<EntryPoint>]
let main args = 
    printfn "Go!"

    let solution = result
    printfn "Found the house: %A" solution
    Console.ReadKey() |> ignore
    0